import React, { Fragment } from "react";
import { RenderProp } from "../../containers/GetFavorites";

const Favs = () => {
  return (
    <Fragment>
      <h2>Favs</h2>
      <RenderProp />
    </Fragment>
  );
};

export default Favs;
